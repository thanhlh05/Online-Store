$kind: http-request
url: "{{base_url}}/cart/B0001"
method: DELETE
auth:
  type: bearer
  credentials:
    token: "{{manager_token}}"
scripts:
  - type: afterResponse
    code: "pm.test(\"Manager khong duoc xoa SP khoi cart - Status 403 Forbidden\", function () {\r

      \    pm.response.to.have.status(403);\r

      });"
    language: text/javascript
order: 6000
