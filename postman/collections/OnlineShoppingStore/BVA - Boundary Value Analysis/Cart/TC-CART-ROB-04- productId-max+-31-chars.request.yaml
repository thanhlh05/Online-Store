$kind: http-request
name: "TC-CART-ROB-04: productId max+ (31 chars) → Reject 400"
url: "{{base_url}}/cart/add"
method: POST
headers:
  Content-Type: application/json
body:
  type: json
  content: |-
    {
      "productId": "AAAAAAAAAAAAAAAAAAAAAAAAAAAAAAA",
      "quantity": 5
    }
auth:
  type: bearer
  credentials:
    token: "{{customer_token}}"
scripts:
  - type: afterResponse
    code: |-
      pm.test("TC-CART-ROB-04: productId max+ (31 chars) → 400", function () {
          pm.response.to.have.status(400);
      });
    language: text/javascript
order: 13000
