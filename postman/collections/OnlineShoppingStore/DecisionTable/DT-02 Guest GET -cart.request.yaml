$kind: http-request
name: DT-02 Guest GET /cart
url: "{{base_url}}/cart"
method: GET
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status code is 401", function () {
          pm.response.to.have.status(401);
      });
    language: text/javascript
order: 6000
