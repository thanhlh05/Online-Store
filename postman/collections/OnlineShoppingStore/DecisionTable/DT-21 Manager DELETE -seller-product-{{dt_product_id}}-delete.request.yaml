$kind: http-request
name: DT-21 Manager DELETE /seller/product/{{dt_product_id}}/delete
url: "{{base_url}}/seller/product/{{dt_product_id}}/delete"
method: DELETE
headers:
  Authorization: Bearer {{manager_token}}
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status code is 200", function () {
          pm.response.to.have.status(200);
      });
    language: text/javascript
order: 25000
