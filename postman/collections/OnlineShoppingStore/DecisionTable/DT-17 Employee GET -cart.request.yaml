$kind: http-request
name: DT-17 Employee GET /cart
url: "{{base_url}}/cart"
method: GET
headers:
  Authorization: Bearer {{employee_token}}
scripts:
  - type: afterResponse
    code: |-
      pm.test("Status code is 403", function () {
          pm.response.to.have.status(403);
      });
    language: text/javascript
order: 21000
