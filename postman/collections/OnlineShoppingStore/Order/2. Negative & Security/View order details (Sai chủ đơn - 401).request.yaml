$kind: http-request
url: "{{base_url}}/order/2147483647"
method: GET
auth:
  type: bearer
  credentials:
    token: "{{customer_token}}"
scripts:
  - type: afterResponse
    code: |-
      pm.test("Hệ thống chặn xem đơn người khác - Status 401 Unauthorized", function () {
          pm.response.to.have.status(401);
      });
    language: text/javascript
order: 1000
