$kind: http-request
url: "{{base_url}}/order/finish/2147483642"
method: PATCH
auth:
  type: bearer
  credentials:
    token: "{{customer_token}}"
scripts:
  - type: afterResponse
    code: |-
      pm.test("Khách hàng không có quyền chốt đơn - Status 403 Forbidden", function () {
          pm.response.to.have.status(403);
      });
    language: text/javascript
order: 3000
