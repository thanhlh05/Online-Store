$kind: http-request
url: "{{base_url}}/product?page=1&size=10"
method: GET
queryParams:
  page: "1"
  size: "10"
scripts:
  - type: afterResponse
    code: |-
      pm.test("Lấy danh sách sản phẩm - 200 OK", function () {
          pm.response.to.have.status(200);
      });
    language: text/javascript
order: 1000
