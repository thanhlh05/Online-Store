$kind: http-request
url: "{{base_url}}/seller/product/D0001/delete"
method: DELETE
auth:
  type: bearer
  credentials:
    token: "{{manager_token}}"
scripts:
  - type: afterResponse
    code: "pm.test(\"Manager xoa san pham thanh cong - 200 OK\", function () {\r

      \    pm.response.to.have.status(200);\r

      });"
    language: text/javascript
order: 6000
